kernel void encompass_triangles(
		global const float *triangle_buffer,
		const uint 			triangle_count,
		global const float *encompassing_aabb
		)
{
#define TRIANGLE_STRIDE 45
#define POS_0 0
#define POS_1 15
#define POS_2 30

	size_t index = get_global_id(0);
}